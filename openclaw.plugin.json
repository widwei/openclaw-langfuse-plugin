{
  "id": "openclaw-langfuse-plugin",
  "name": "OpenClaw Langfuse Plugin",
  "description": "Sends agent traces and LLM generations to Langfuse for observability. Uses llm_input/llm_output hooks for accurate model, provider, and token tracking.",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string"
      },
      "publicKey": {
        "type": "string"
      },
      "secretKey": {
        "type": "string"
      }
    }
  },
  "uiHints": {
    "baseUrl": {
      "label": "Langfuse Base URL",
      "help": "Langfuse server URL (e.g. https://cloud.langfuse.com or http://localhost:3000)"
    },
    "publicKey": {
      "label": "Public Key",
      "help": "Langfuse project public key (pk-lf-...)"
    },
    "secretKey": {
      "label": "Secret Key",
      "help": "Langfuse project secret key (sk-lf-...)",
      "sensitive": true
    }
  }
}
